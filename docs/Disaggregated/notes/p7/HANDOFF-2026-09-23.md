# P7 集成交接（2026-09-23，p7w6 矩阵续跑中）

> 从 [`HANDOFF-2026-09-22.md`](HANDOFF-2026-09-22.md) 续接。本页只记录本轮之后的变化；历史裁定仍见 `INTEGRATOR-DECISIONS-P7.md`，债见 `MG_Remote/CONTRACT-P7.md` §12。集成树只在 WSL `~/w7/pipe` 操作 Git；Windows 主 dev 树不可触碰，Windows 侧只用 `MobileGL-disagg` worktree 按 SHA 快进和推送。所有提交不得有 `Co-Authored-By` 尾注。

## 当前树与设备

- WSL pipe：M3 已 cherry-pick 到 `9fd768d4`，G1 / 静态门 / 八车道 / verify / OpenRA 回放全部绿；M3 集成者源码审查为 **land**，见 `docs/Disaggregated/notes/handoff/review-m3.md`。其后有一处只改注释的未提交修正和待同步文档。`origin/feat/disaggregated` 最后已推的头是 `f749d4c3`（APK 归档显式路径修复），**M3 尚未推**。
- 手机 Redmi `2f7cbe2e` 已装 p7w6 APK：stamp `p7w6-0e16ca27`，签名 SHA `45759c30…`，包 `top.mobilegl.plugin.p7w1.trace`；supervisor `0.0.0.0:40613`，token `p7w1b-lan-token-20260922`，`svc power stayon true` 后 `stay_on_while_plugged_in=15` 且 Awake。WSL 路由 `192.168.21.181/32 via 192.168.31.1 dev eth0` 在，手机 Wi-Fi IP 仍是 `192.168.21.181`。该 APK 基于 M3 之前的源码，后续 M3 设备回归须另建下一版。
- p7w6 bsl-esc-menu-854 DirectVulkan × pbuffer：spawn/inproc 各三遍 6/6 过，六张实际图像逐位相同且与旧 monolith SHA 相同，见 `device-window-1/W6-verify/`；本轮没有采集 `wbuf[]` 峰值。主机 M3 之后的 bsl inproc/spawn 均为既有 `ssim=0.998402`、0 Fatal；确有一次 M3 回卷，spawn `wbuf[wlivepk=1052 wdefsync=22]`，日志 `~/w7/logs/p7m3-bslhost/`。

## 正在跑的手机批次

- DirectGLES × TCP 的 38 例：旧 p7w5 批次真实分母 38，第一遍 11/38 绿、第二遍只跑完 13 个就因 adb 断开中止；报告 `docs/Disaggregated/notes/p65/window-1b-p7w5.md`。
- p7w6 第一段 `~/w7/logs/p7w6-matrix/pass1/` 有前九例终局结果（8 绿、`1.21.11-main-menu` 内部约 302 s 超时）；`rei-inventory` 与 `xaero-minimap` 被**外层 900 s** 截断，原始日志保留，不是产品判定。续跑脚本 `~/w7/logs/p7w6-matrix-resume.sh` 用外层 1900 s（清单最大 1800 s），跳过已终局结果，产物 `~/w7/logs/p7w6-matrix-resume/pass1/`，进度 `~/w7/logs/p7w6-matrix-resume.log`，由 `p7w6-matrix-launch.sh` 以 setsid/nohup 启动。写本页时正重跑 `rei-inventory`，手机 child 有持续帧进度。若它中断，先确认 adb / supervisor / 路由，再运行 launcher；脚本会跳过已有 `results.json` 的例子。不要同时跑真机 gate3/CTS：`run_android_retrace_local.py` 会 force-stop 同包 Service。

## 包树

- `~/w7/p7-m3`：包提交 `7acad6b8` 已拣到 pipe `9fd768d4`。2048 次 descriptor Acquire 的长帧边界先 flush 当前录制、等 graphics queue idle，再回卷每个 layout cursor 与 memo。2049 个 SSBO 窗口用例 split/spawn 绿；禁用回卷两臂红；pull `.text=0xa52203`、符号 0/0，棘轮 173、parity 0，pipe 完整门 unit 2436 / split 304 / spawn 220 / tcp 223 / magma 102/81/71 / full 540 / verify 1152/1086。review 文本已复制到 `~/w7/notes/handoff/review-m3.md`。集成文档与推送待做。
- `~/w7/p7-b4`：**草稿，未落地**。聚合 fence 等待、frame serial 重扫、Present 槽回收前等待、pre-pass 计入 pending、wire 路径每 256 draw 的 texture prune、`AdoptRun` extent 变更清旧 bytes/coverage。构建、Magma 101/80/71、full 539、OpenRA 两臂、G1、普查/棘轮/parity 绿；`AdoptRun` 单测红一次 / 恢复绿。**仍缺聚合等待的可证伪负控、完整八车道与 verify、pipe 源码审查和与 M3 的 VulkanRenderer 冲突合并**。不要按现有绿数直接推。详情 `docs/Disaggregated/notes/p7/magma-b4.md`（仅包树）。

## 下一步

1. 等 p7w6 的 38 例对照结束，逐例合并两段结果，判断 DataBind 配对与息屏假设；补 `notes/p65/` 报告、ID、CONTRACT-P7 §7。旧驱动的两个外层截断不得计入产品红数。
2. 完成 M3 的 docs / ID / CONTRACT / ROADMAP 同步，`check_doc_citations.py --strict`，Windows worktree `git fetch wslpipe feat/disaggregated` → `git merge --ff-only <pipe-sha>` → `git push origin <pipe-sha>:refs/heads/feat/disaggregated`。只按 SHA；不碰 Windows 主 dev 树。
3. 手机空闲后做门 3 终局：同会话 36 例 × DirectVulkan × pbuffer × inproc/spawn × 3 遍、逐位一致且对 monolith 差 ≤ 0.0005，OpenRA 必须 1.0；`MOBILEGL_TRACE_APK` 显式指向签名 APK（`f749d4c3` 修复归档错 SHA）。再开 `MOBILEGL_PIPE_STATS=1` 专门采一次 bsl maps 峰值与 `wbuf[]`。随后 CTS AFTER 五块与 `$BASE` 比 ≤0.5 pp、新 crash 0。
4. B4 补确定性负控并完整审查、落地；F2 对端畸形记录驱动及 D11/PH-2/PH-6/PH-1/PH-7；wave 3 棘轮余 88；wave 5 fuzz 三臂、异模型整体审查。M3/B4/F2 等落地后须用新 APK 做必要的真机回归，不能把 p7w6 旧二进制的门 3 结果当最终版证明。
