# P7 集成交接（2026-09-23，p7w6 矩阵与 B4 pipe 门已结束）

> 从 HANDOFF-2026-09-22.md 续接。集成树只在 WSL ~/w7/pipe 操作 Git；Windows 主 dev 树不可碰，Windows 侧只在 MobileGL-disagg worktree 按 SHA 快进和推送。提交不得带 Co-Authored-By 尾注。

## 当前树与设备

- WSL pipe 当前 HEAD 为 b19297d5：已含 M3、B4；B4 集成源码审查 land，完整 pipe 门通过。origin/feat/disaggregated 目前仍在 7ea7ce89；B4 及本页等文档收尾后按 SHA 快进和推送。
- p7w6 APK stamp p7w6-0e16ca27，早于 M3 与 B4；手机矩阵已跑完且没有后续设备门启动。设备侧 retrace 后续必须使用最终功能 APK，不能把 p7w6 当 M3/B4/F2 证明。
- DirectVulkan bsl-esc-menu pbuffer inproc/spawn 各三遍 6/6，六张图与 monolith PNG SHA 一致；本窗口没有采集 maps 行数和 wbuf[] 峰值。

## p7w6 DirectGLES × TCP 窗口 1b

- 真实分母 38：9 个已有终态缓存跳过（8 pass、1 idle timeout）；本次尝试 29 个，16 pass、8 case-level idle timeout、2 visual fail、3 missing-result timeout。逐例 armed / reap / SSIM / timeout 记于 notes/p65/window-1b-p7w6.md；原始日志、results/checkpoints、client/server logs、phone logcat、diff PNG 位于 ~/w7/logs/p7w6-matrix-resume/pass1/。
- 视觉失败：Iris Photon v1.3b（0.008877075，近乎黑图，服务器 shader varying link 失败）和 Iris Derivative Main D24.4.14（0.850784981，部分场景错绘）。二者都应做同设备 DirectGLES monolith 对照，确认是否由 split 路径引入后再修；矩阵终态记录 child exit=0 / sessionsFaulted=1。
- missing-result：REI inventory、Xaero minimap in-world、ModernUI inventory。前两例为外层 runner rc=124；ModernUI 到 1900s 外层上限，checkpoint cancelled/child rc=130。不能把这三项算成 visual product failure。
- 8 个 case-level idle timeout：REI/Xaero/JourneyMap/ModernUI normal-world、Create indirect、Improved Transparency、Iris BSL ESC menu 等；部分 phone reap 日志未知。若重跑，逐例保留日志并先降低 host CPU 竞争；idle timeout 和 missing-result 分开记录。B4 host gate 与未知子集矩阵尝试有时间重叠，timeout 墙钟可能受影响。

## 已完成包

- M3：pipe 9fd768d4，ID-P7-50、review-m3 land、完整门已绿；文档与推送已在 3ce5b991 / 7ea7ce89 完成。
- B4：pipe a4c1bc8b / b19297d5，ID-P7-51、review-b4 land。聚合 fence prefix、frame serial 重扫、present slot 回收前等待、pre-pass pending、texture prune、staged extent 清理；deterministic aggregate-wait control 已绿。pipe full gate：G1 .text 0xa52203 / symbols 0/0，census79、ratchet173、parity/audit绿，unit2439、split304、spawn220、tcp223、Magma102/81/71、full540、verify1152/1086，OpenRA两臂1.0 / 0 unsound。日志 ~/w7/logs/p7b4-integration-b19297d5-rerun-20260923/。
- F2 package p7-f2-driver：raw peer / D11 CSO slot commit db80dd16；PH-3 readback reply bound commit adf2cbe0。前两切片定向测试和 G1 已过。PH-4 staged texture byte bound 仍在 package diff；独立负控曾先触发 ResourceSubData.LevelExtent，已定位为 fixture 未按生产格式打包 respecify upload target，正修正后重验。

## 下一步

1. 完成本页其余文档更新、check_doc_citations.py --strict，再从 MobileGL-disagg 按 SHA 快进到 pipe HEAD b19297d5 并推送 origin/feat/disaggregated；不触碰 Windows 主 dev 树。
2. 等 PH-4 raw peer + Store unit 独立 Luna 验证通过后 commit package；继续 PH-5 ArchiveVector、PH-2 SlotTables、PH-6、PH-1 (3)(4)、PH-7 (5)，之后做 F2 完整包门与集成源码审查。
3. 同一设备对 Photon v1.3b 与 D24.4.14 做 DirectGLES monolith 对照；视日志/对照结果再决定修复。必要时在无 host gate 的窗口按需重跑 idle timeout/missing-result 项。
4. 功能包完成并部署后做门 3：36 例 DirectVulkan pbuffer × inproc/spawn，三遍逐位一致并与 monolith 同会话差 ≤0.0005，OpenRA 1.0；再采 MOBILEGL_PIPE_STATS 下 bsl maps/wbuf[] 峰值。CTS AFTER 五块与 BASE 比 ≤0.5 pp、新 crash 0。
5. wave 3 剩余 ratchet 88；wave 5 fuzz 三臂、P7 收官异模型整体审查。

无当前需要用户决策的阻塞；设备验证由 Luna 代理执行，避免与测试相互干扰。
