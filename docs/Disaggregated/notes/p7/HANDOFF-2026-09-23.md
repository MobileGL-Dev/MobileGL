# P7 集成交接（2026-09-23 收官）

> 接 [HANDOFF-2026-09-22.md](HANDOFF-2026-09-22.md)。**P7 已收官**：CONTRACT-P7 §8 的 9 项分母全绿，门 4 以 `# P13` 读法关闭（ID-P7-55）。铁律不变：Git 只在 WSL `~/w7/pipe` 与各 `~/w7/p7-<slug>` 包树操作；Windows 只在 `MobileGL-disagg` 按 SHA 快进、推送；提交不带任何尾注；不对运行中的 workflow 子代理 SendMessage（会另起副本抢同一包树）。

## 1. 现状

- `origin/feat/disaggregated` = `~/w7/pipe` = 本页所在提交。今天落地（ID-P7-55..63）：CI 七根因修 + 80 个标题缩短 + 并发组（`1997fd88` 上 Test 首次全绿，含 P6.5 以来首次跑的 retrace 链）；ratchet88（棘轮 171）；F2（PH-1 (3)(4) 每会话闩、PH-6、PH-7 (5) fork 前认证、fuzz 三臂）；门 5 三缺陷（ReadPixels 切带、深度 resolve 臂设备探针、越界图像单元占位）；codex 收官审查修复；spawn 冷启动心跳（控制协议修订 **2**）；门 3 分布判据 + 1:1 blit 最近邻。
- 最终 v2 门（`p7/int3@74636c84` + 工装 / 文档）：G1 `.text` 0xa52203 / 符号 0/0；census 79；棘轮 171；parity 0；hygiene 14/14；unit 2568、split 322、spawn 239、tcp 243、magma 125 / 104 / 89 / 558、integration-gpu 2188 × 2、verify 2568 / 1188 / 1122；OpenRA 两臂 1.000000。
- **终局设备窗口 3**（`p7w8-78e71f2b`，Redmi 2f7cbe2e，reboot-clean、pinned，证据 `notes/p7/device-window-2/final-p7w8/`）：**门 3 PASS 36/36**（33 例四臂逐位同；三例 Iris monolith 自身不稳定 → ID-P7-62 分布判据通过；create-instancing 已与 monolith 同图；OpenRA 四臂 1.000000）；**门 5 PASS**（shader-image +4.35 pp、texture +0.96 pp、其余持平，0 新 crash，0 流失）；bsl spawn server 360 MB / 10,330 映射行。
- 手机：装 `p7w8-78e71f2b`（包 `top.mobilegl.plugin.p7w1.trace`），supervisor 已按 `90-restore.sh` 复原；`/data/local/tmp/mgcts` 已放回 `$BASE` 库。**控制协议修订 2：旧 APK 的 server 与新 client 不兼容**。

## 2. 工装

- 本地门 v2：`~/w7/logs/p7-gate-template2.sh`（`sed 's#~/w7/pipe#<tree>#; s/TAG/<tag>/'`）——比 v1 多 pipe-gates 全步、integration-gpu 双传输（CI 的三个 iterationrp 旋钮，**别用** `R11G11B10F_FALLBACK`）、车道去 `DISPLAY`。
- 设备窗口：`bash ~/w7/notes/p7/window2/detach.sh <log> ~/w7/notes/p7/window2/window.sh <pipe-sha> [<stamp>]`，约 1.6 h（门 3 monolith ×5），可续跑；RUNBOOK 在 `notes/p7/device-window-2/tools/RUNBOOK.md`。
- 主机 CTS：`~/w7/logs/int2a/cts/cts1.sh`（主机 glcts + `LD_PRELOAD`，pbuffer，核对 renderer 串）。

## 3. 余项（都不是 P7 的出口门）

1. CONTRACT-P7 §12 的记录债（含 ID-P7-58/60 新增：图像单元同单元写后读残余、探针形状覆盖、非 1:1 深度 blit 无 `BLIT_DST`、`appliedSeq` 前进、PH-5 只管 vector、暂存纹理尾写整层零填等）。
2. CI：`concurrency` 组下旧的排队 run 不总被自动取消（本会话手工取消过两次）；观察一两轮再决定是否改为按提交取消。
3. 下一阶段按 ROADMAP：monolith 跑道 **P8**（emulation 下放 + 索引宿主镜像），IPC 跑道 **P9**（反向通道异步 reply）→ P10 → P11 → **P12**（Android 生产窗口路径；Ph 已清）。P3b/P4b 余项（场景普查后半）并行。
