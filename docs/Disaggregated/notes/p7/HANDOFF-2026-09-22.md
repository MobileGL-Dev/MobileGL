# P7 集成交接（2026-09-22 夜，集成者会话收尾）

> 这一页是给接手 agent 的**机械清单**：哪些已落地、哪些在跑、哪些没做完、每一步的命令。裁定与数字见
> `INTEGRATOR-DECISIONS-P7.md`（到 ID-P7-43）、`CURRENT_STAGE_STATUS.md`、`MG_Remote/CONTRACT-P7.md`。

## 0. 铁律（不变）
- 只在 WSL `~/w7/pipe`（集成树）和各 `~/w7/p7-<slug>` 包树工作；**绝不碰** Windows 主 dev 树 `…\FoldCraftLauncher\MobileGL`；Windows 侧只用 worktree `…\MobileGL-disagg`。
- **绝不从 Windows 经 UNC 对 `~/w7/pipe` 跑 git**（会留下 `.git/index.lock`）；git 只在 WSL 里跑。
- 推送只按 SHA：`git fetch wslpipe feat/disaggregated && git merge --ff-only FETCH_HEAD && git push origin <sha>:refs/heads/feat/disaggregated`（Windows worktree）。
- **提交禁止任何 `Co-Authored-By` 尾注**（用户明令；`scratchpad/pick.sh` 在 `--amend` 时自动剥）。
- 性能只记录不做门；不顺手修 `dev` 的 bug；每包一次 fable 审查；rule I / J / G1 / G14 见 CONTRACT-P7 §0。
- 给子代理的 prompt 必须先写明层级（常设指令 + 「最新一句已由集成者执行」），否则它们会把用户最新一句当唯一指令拒做；需要 Opus 5.5 时走 Workflow `model:"claude-opus-5-5"`（Agent 工具的 `opus` 仍是 Opus 5），本轮因拒做问题全部改走 Agent 工具 + fable。
- `wsl.exe -d Arch -- bash -lc '…$x…'` 会吞 `$var`：**一律写脚本文件**再 `MSYS_NO_PATHCONV=1 wsl.exe -d Arch -- bash /mnt/c/…/x.sh`；长输出别 `| head`（SIGPIPE 会杀脚本），重定向到日志再看。

## 1. 现状（本会话结束时）
> **2026-09-22 深夜补记（ID-P7-44..48）**：M2 r2、X2、V1 修复 r2 都已落地并推送——`origin/feat/disaggregated` = V1 修复 r2 的头 `336174e3` 加其上的这笔 docs 提交；§2 表中 M2 审查 / M2 r2 / F 修复轮审查 / B3 r3 + B2 r4 审查 / V1 修复 r2 / X2 各行**已完成**（审查文本在 `notes/handoff/review-*.md`），§4 第 1 项完成；wire 臂的 memo ABA 已关。窗口 1b 矩阵仍在跑：中期读数与「手机 60 s 息屏切断 child TCP」的假设、以及重跑前该做的 `svc power stayon true`，见 ID-P7-48——写报告的人请先看它。

- `origin/feat/disaggregated` = **`87584d0b`**（尾注已改写；B2 r1/r2、B3 返工 + 修复轮、V1、F 片 1–3、E1）。
- 集成树 `~/w7/pipe` 头 = 见 `git log`（B3 修复 r2/r3、V1 修复轮、B2 r3/r4、M2、F 修复轮、docs 到 ID-P7-43）；每一步的门都全绿（唯一红 = `InitialCapsStartup.ANullInitialSnapshot…` flake，X2 在修）。**推送时机**：接手者先跑一次 `~/w7/logs/p7-gate-template.sh`（`sed s/TAG/<tag>/g`），绿即推。
- 手机（Redmi 2f7cbe2e）：装的是 **p7w5**（`top.mobilegl.plugin.p7w1.trace`），supervisor `0.0.0.0:40613`，token `p7w1b-lan-token-20260922`；主机侧同 stamp 制品 `~/w7/logs/p7w5host/host/`（树 `~/w7/p7-w5host`）。
- 用户已加 WSL 路由 `192.168.21.181/32 via 192.168.31.1 dev eth0`（重启 WSL 会丢，需重加）。

## 2. 在跑的 agent（结果会写到 `~/w7/notes/handoff/`）
| 名 | 树 / 产物 | 接手动作 |
|---|---|---|
| M2 审查 | `~/w7/notes/handoff/review-m2.md`（verdict land with fixes，**一条 must-fix**：memo 句柄复用 ABA） | 已派 **M2 r2**（fable）修；结果 `~/w7/notes/handoff/m2-r2.md` |
| M2 r2（fable） | `~/w7/p7-magma-m2` 在 `6f67fd7f` 之上的新提交 | cherry-pick → 门 → 审查 → 推 |
| F 修复轮审查 | `~/w7/notes/handoff/review-f-fix.md` | 按 verdict 派修复轮或落地 |
| B3 r3 + B2 r4 审查 | `~/w7/notes/handoff/review-b3r3-b2r4.md` | 同上 |
| V1 修复 r2（fable） | `~/w7/notes/handoff/v1-fix-r2.md`，树 `~/w7/p7-verify-split` 在 `723bb93f` 之上 | cherry-pick → 门（含 verify 构建）→ 审查 |
| X2 flake（fable） | `~/w7/notes/handoff/x2-caps-flake.md`，树 `~/w7/p7-x2-caps-flake` | cherry-pick → unit 门 |
| 窗口 1b 矩阵 | `~/w7/logs/p7w1b/paced.log`（`=== ALL DONE` 结束）、`~/w7/logs/p7w1b/paced/pass{1,2}/<case>/`（driver.log、client log、phone-logcat.txt） | 写 `notes/p65/window-1b-p7w5.md`（每例状态 / 原因 / 两遍是否一致；已知：main-menu 类 server 死亡、OpenRA 偶发 `ReadbackDeclined`、1.21.11-main-menu 超时）+ ID + CONTRACT-P7 §7 |

## 3. 机械步骤（每包相同）
```bash
# 1) cherry-pick（自动剥尾注；有冲突会停）
bash ~/w7/notes/tools/pick.sh <worktree-name> <base-sha> <sha…>       # 脚本在本会话 scratchpad，或重写：逐个 cherry-pick + amend 掉 Co-Authored-By
# 2) 门（模板：build-split + build-linux G1 + census + ratchet + parity + 八车道 + 审计 + OpenRA split 回放 + verify 构建）
sed 's/TAG/<tag>/g' ~/w7/logs/p7-gate-template.sh > ~/w7/logs/p7-gate-<tag>-inner.sh
sed -i '2i export MOBILEGL_FLATC_EXECUTABLE=/home/swung/w7/flatc-build/flatc' ~/w7/logs/p7-gate-<tag>-inner.sh
rm -f ~/w7/pipe/build-split/Testing/Temporary/LastTestsFailed.log
setsid nohup bash ~/w7/logs/p7-gate-<tag>-inner.sh > ~/w7/logs/p7-gate-<tag>.log 2>&1 < /dev/null & disown   # 等 "=== DONE-VERIFY"
# 3) 审查：fable、只读、按 file:line 出 verdict（land / land with fixes / rework）
# 4) 文档：INTEGRATOR-DECISIONS 加一行 ID、CURRENT_STAGE_STATUS §1/§2/§4/§5、ROADMAP P7/Ph/P3b 行、CONTRACT-P7 §12 抄包 note 的债；
#    python3 scripts/check_doc_citations.py --rev HEAD --strict docs/Disaggregated/*.md docs/Disaggregated/notes/p7/INTEGRATOR-DECISIONS-P7.md MobileGL/MG_Remote/CONTRACT-P7.md
# 5) 推送（Windows worktree）：git fetch wslpipe feat/disaggregated && git merge --ff-only FETCH_HEAD && git push origin $(git rev-parse HEAD):refs/heads/feat/disaggregated
```
pipe 的 tcp fixture 端口是 40713（`build-split`）、40714（`build-verify`），别改回 40613（会撞包树的 fixture）。

## 4. 没做完的（按优先级）
1. **M2 r2 must-fix 落地**（见 §2）——在它落地前，wire 臂有一处已知的中帧 use-after-free 可达点（memo 按裸 VkBuffer 句柄命中）。
2. **p7w6 APK + 手机 server 重部署**：`sed 's/p7w5/p7w6/g' ~/w7/logs/p7w5-apk-inner.sh > ~/w7/logs/p7w6-apk-inner.sh`（在 pipe 头上跑；stamp `p7w6-<head>`；产物 `~/w7/logs/p7w6/trace-p7w6.apk`；`adb install -r`；`tcp_device_server.py start …`（见 `supervisor-start.sh`，改 token ≥16 字节）。**F 改了 wireFingerprint：旧 server + 新 client 只会 `no Welcome (rc=6)`**。然后：bsl-esc-menu-854 DirectVulkan pbuffer spawn + inproc 各 3 遍（M2 的设备验收，记 maps 与 `wbuf[]` 行）、窗口 1b 用 p7w6 重跑一遍（对照 `DataBind` 是否消除 250 ms 配对窗口）、门 3 终局（分母 36：三遍逐位 + spawn 臂同会话，`run_android_retrace_local.py --matrix --backend DirectVulkan --use-pbuffer --transport inproc|spawn --repeat 3`）。
3. **M3**：descriptor set 在长帧内无界（`UniformManager::AcquireDescriptorSet` 只在 `BeginFrame`/drain 回卷）——按 submit-index 证明回卷每个 layout 的游标；`review-m2.md` 域外 (b)。
4. **B4**：`WaitForSubmitsUpTo` 聚合等待 + `WaitForFrameSerial` 的「答 true 而未完成」（restart 不 continue，且不再读 `record`）+ 两处陈旧注释 + `CollectGarbage`/`pruneWire` 在 wire 臂从不执行（`VulkanRenderer.cpp` `SetupDraw`）+ §12 裁判点名的 Magma 债。
5. **F2**：D11 五处 + PH-2、PH-6 drop-with-latch、PH-1 (3)(4)、PH-7 (5) fork 前认证——前置是能向 spawn/TCP server 投递畸形记录的对端字节驱动（fuzz 臂 2 第一块）；`ServerSpawn.cpp` 清洗名单补 `Ipc.SpinUs/ServerAffinity/Audit`（M2 r2 顺带）。
6. **wave 3**：棘轮 88 = monolith draw 路径的 `#if`（`link_ratchet.py --bucket`，`notes/p7/magma-b2.md` §4.3 逐符号点名）。
7. **wave 4**：CTS AFTER（inproc × DV 五块 vs `notes/p7/device-window-1/CTS-base/`，≤0.5 pp，新增 crash = 0；`tools/cts/caselists/`，设备 `/data/local/tmp/mgcts`）。
8. **wave 5**：fuzz 三臂、P7 收官异模型整体审查（ID-66）。
9. 小件：`retrace_gate.py` 只覆盖 monolith 条目——pipe 门现用 `~/w7/logs/pipe-retrace.sh`（借 `~/w7/p7-magma-b3/build-split/tools/trace_replay/mobilegl_trace_replay`）；把 `MOBILEGL_BUILD_TRACE_REPLAY=ON` 配进 pipe 的 `build-split` 后可删这层借用。`P5D-INPROC-PERFORMANCE.md` 的 Adreno 数字重取（§12）。测量树 `~/w7/m2-meas*`、`~/w7/rev-*` 可删。
10. 文档同步：Claude Docs 主进度页（`34576d4c-…`）每次落地一段；记忆文件 `disaggregated-plan.md`。
