# citation self-test

Good, resolvable by basename: `DirectGLES.cpp:2916` and `RenderState.h:263`.
Good, full path with a range: `MobileGL/MG_Backend/BackendObject.h:302-522`.
Bad, past the end of the file: `SamplerObject.h:468-492`.
Bad, no such file at this revision: `NoSuchThing.cpp:12`.
Bad, inverted range: `RenderState.h:400-100`.
